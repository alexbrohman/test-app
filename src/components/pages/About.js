import React from "react"
import ScrollDiv from "../utilities/ScrollDiv"

const About = () => {
    return (
        <ScrollDiv>
            <h1>About</h1>
        </ScrollDiv>
    )
}

export default About
